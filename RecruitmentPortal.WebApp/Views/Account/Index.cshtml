@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    int rowcount = 0;
}

@model IEnumerable<RecruitmentPortal.Core.Entities.ApplicationUser>
@*//Added search box test*@
<div class="text-center">
    <h1 class="display-4 text-center"><b>All Interviewers</b></h1>

    <form asp-controller="Account" asp-action="Index" method="get">
        <p>
            Email / Position / Skype_Id : <input type="text" name="SearchString">
            <input class="btn btn-primary" type="submit" value="Search" />
        </p>
    </form>
</div>
<div class="d-flex justify-content-end">
    <a asp-controller="Account" asp-action="Register" class="btn btn-info justify-content-end mr-3">Add Interviwer</a>
</div>
<br />
    <table class="table table-sm table-bordered table-striped">
        <tr><th>S.No.</th><th>Name</th><th>Email</th><th>Position</th><th>Skype-ID</th><th>Edit</th><th>Delete</th></tr>
        @foreach (var user in Model)
        {
            <tr>
                <td>@(rowcount+=1).</td>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>@user.position</td>
                <td>@user.skype_id</td>
                <td>
                    <form asp-action="UpdateUser" asp-route-id="@user.Id" method="post">
                        <button type="submit" class="btn btn-sm btn-primary">
                            Edit
                        </button>
                    </form>
                </td>
                <td>
                    <form asp-action="DeleteUser" asp-route-id="@user.Id" method="post">
                        <button type="submit" class="btn btn-sm btn-danger">
                            Delete
                        </button>
                    </form>
                </td>

            </tr>
        }
    </table>
