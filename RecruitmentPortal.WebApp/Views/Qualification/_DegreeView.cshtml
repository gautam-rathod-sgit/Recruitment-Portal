
@model RecruitmentPortal.WebApp.ViewModels.DegreeViewModel

@using (Html.BeginForm("AddNewDegree", "Qualification", FormMethod.Post, new { role = "form", id = "degreeform" }))
{
    <input type="hidden" value="@Model.ID" id="ID" name="ID" />
    @Html.HiddenFor(m => m.isActive)
    <div class="form-group">
        @Html.LabelFor(m => m.degree_name, new { @class = "asterisk" })
        @Html.TextBoxFor(m => m.degree_name, new { @placeholder = "Degree Name", @class = "form-control" })
        @Html.ValidationMessageFor(m => m.degree_name, "", new { @class = "text-danger" })
    </div>

    <div class="modal-footer">
        <button type="submit" class="btn btn-primary" onclick="return checkFormValid()">Save</button>
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
    </div>
}


<script>
    $(document).ready(function () {
        $.validator.unobtrusive.parse($('#degreeform'));
    });

    function checkFormValid() {
        var $form = $('form');
        if ($form.valid()) {
            return true;
        }
        return false;
    }
</script>